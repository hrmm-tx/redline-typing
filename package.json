{
  "name": "redline-backend",
  "version": "1.0.0",
  "description": "High-performance backend for Redline Speed Typing",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "migrations:apply": "wrangler d1 migrations apply DB --remote",
    "migrations:local": "wrangler d1 migrations apply DB --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251217.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.57.1"
  }
}
